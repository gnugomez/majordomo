{
  "name": "majordomo",
  "version": "0.2.0",
  "description": "macOS menu-bar inbox that unifies GitHub and self-hosted GitLab mentions",
  "private": true,
  "license": "GPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/gnugomez/majordomo.git"
  },
  "main": "dist/main/index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "npm run build && electron .",
    "typecheck": "tsc --noEmit",
    "icons": "electron scripts/render-tray-icons.cjs",
    "icons:ico": "node scripts/make-ico.mjs",
    "package": "npm run build && node scripts/make-icns.mjs && node scripts/package.mjs",
    "package:win": "npm run build && node scripts/package.mjs --platform=win32 --arch=x64",
    "package:linux": "npm run build && node scripts/package.mjs --platform=linux"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "@gitbeaker/rest": "^43.8.0",
    "@octokit/rest": "^22.0.1",
    "@types/node": "^26.3.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "electron": "^44.0.0",
    "electron-squirrel-startup": "^1.0.1",
    "electron-winstaller": "^5.4.4",
    "esbuild": "^0.28.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^7.0.2"
  },
  "optionalDependencies": {
    "electron-liquid-glass": "^1.1.1"
  }
}
