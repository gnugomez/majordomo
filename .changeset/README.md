# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).
Run `npx changeset` to describe your change; the release pipeline turns pending
changesets into a version bump, a changelog entry, and a GitHub release. See
[RELEASING.md](../RELEASING.md) for the full flow.
