name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: swift build
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v5
      - run: swift build
